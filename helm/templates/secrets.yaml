apiVersion: v1
kind: Secret
metadata:
  name: {{ include "dclaw-slide.fullname" . }}-secrets
type: Opaque
stringData:
  DATABASE_URL: "postgresql+asyncpg://postgres:postgres@postgres:5432/dclaw_slide"
  APP_ENV: "production"
  SECRET_KEY: "change-me-in-production"
  OLLAMA_URL: "http://ollama:11434"
  OPENROUTER_API_KEY: ""
