apiVersion: v1
kind: Service
metadata:
  name: {{ include "dclaw-slide.fullname" . }}-backend
  labels:
    app: {{ include "dclaw-slide.name" . }}
    component: backend
spec:
  type: {{ .Values.service.backend.type }}
  ports:
    - port: {{ .Values.service.backend.port }}
      targetPort: 8021
      protocol: TCP
      name: http
  selector:
    app: {{ include "dclaw-slide.name" . }}
    component: backend

---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "dclaw-slide.fullname" . }}-frontend
  labels:
    app: {{ include "dclaw-slide.name" . }}
    component: frontend
spec:
  type: {{ .Values.service.frontend.type }}
  ports:
    - port: {{ .Values.service.frontend.port }}
      targetPort: 3021
      protocol: TCP
      name: http
  selector:
    app: {{ include "dclaw-slide.name" . }}
    component: frontend
